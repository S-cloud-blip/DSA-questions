Check if the array is sorted (31/07/2023, coding ninja question)

Approach - Check if the array is in ascending order i.e. arr[i+1]>arr[i]
Time complexity - O(n)
Space Complexity - O(1)

#include<iostream>
#include<string.h>
using namespace std;

void isSorted(int n, int a[n]) {
    // Write your code here.
    for(int i=1;i<n;i++){
        if(a[i+1]>=a[i])
        {
            
        }
        else{
            cout<<"array is not sorted";
        }
        cout<<"array is sorted";
    }
   
}
int main() {
    int n=26, a[26]={8,3,4,6,6,7,9,7,4,0,5,5,7,1,5,3,4,3,0,6,4,7,1,0,9,4};
    
    isSorted(n,a);
    
}
